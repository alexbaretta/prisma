datasource db {
  provider = "mysql"
}

generator client {
  provider = "prisma-client-js"
  output   = "@prisma-lossless/client"
}

model User {
  id    String  @id @default(uuid())
  email String  @unique
  name  String?
  info  Json?
}
