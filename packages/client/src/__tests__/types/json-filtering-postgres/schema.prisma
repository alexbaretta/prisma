datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "@prisma-lossless/client"
}

model User {
  id    String  @id @default(uuid())
  email String  @unique
  name  String?
  info  Json?
}
