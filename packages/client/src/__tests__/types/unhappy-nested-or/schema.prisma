datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "@prisma-lossless/client"
}

// / User model comment
model User {
  id    String  @default(uuid()) @id
  email String  @unique
  name  String?
  info Json?
}
